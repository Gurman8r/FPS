namespace FPS
{
    public enum TriggerType
    {
        // Units
        OnSpawn = 0,
        OnDeath,
        OnReceiveDamage,
        OnReceiveHealing,
        OnDoDamage,
        OnDoHealing,
    }
}
